<%= form_with(model: grocery_category) do |form| %>
  <% if grocery_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(grocery_category.errors.count, "error") %> prohibited this grocery_category from being saved:</h2>
      <ul>
        <% grocery_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :name %>
  </div>
  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
