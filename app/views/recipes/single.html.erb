<div class="page-container">
  <div class="grid mb-16">
    <%= turbo_frame_tag "single_recipe_#{params[:id]}", src: single_post_recipes_path(params[:id]), loading: :lazy do %>
      <%= render "shared/skeleton_recipe_show" %>
    <% end %>
  </div>
</div>
