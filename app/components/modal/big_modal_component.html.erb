<%= turbo_frame_tag "modal" do %>
  <%= tag.div data: { controller: "turbo-modal",
                      turbo_modal_target: "modal",
                      action: "turbo:frame-render->turbo-modal#open turbo:submit-end->turbo-modal#submitEnd keyup@window->turbo-modal#closeWithKeyboard click@window->turbo-modal#closeBackground" },
                      id: "recipeModal",
                      class: "transition" do %>
    <div class="hs-overlay w-full h-screen top-0 left-0 overflow-x-hidden overflow-y-hidden  duration fixed inset-0 z-40 bg-gray-900 bg-opacity-50 dark:bg-opacity-80 hs-overlay-backdrop">
      <%# <div id="hs-bg-gray-on-hover-cards" class="hs-overlay w-full h-full fixed  left-0 z-[60] overflow-x-hidden"> %>
      <%# <div class="hs-overlay-open:mt-7 hs-overlay-open:opacity-100 hs-overlay-open:duration-500 mt-0 ease-out transition-all lg:max-w-6xl lg:w-full m-3 lg:mx-auto"> %>
      <div class="lg:max-w-6xl  max-h-[90vh] my-20 mx-4 lg:mx-auto flex flex-col bg-white border shadow-sm rounded-md dark:bg-gray-800 dark:border-gray-700 relative">
        <div class="flex justify-between items-center py-3 px-4 border-b dark:border-gray-700 ">
          <h3 class="font-bold text-gray-800 dark:text-gray-200 text-xl">
            <%= title %>
          </h3>
          <%= link_to '',  data: { action: "turbo-modal#hideModal" }, type: "button",  class: "inline-flex flex-shrink-0 justify-center items-center h-8 w-8 rounded-md text-gray-900 hover:text-gray-400 focus:outline-none focus:ring-2 focus:ring-gray-400 focus:ring-offset-2 focus:ring-offset-white transition-all dark:focus:ring-gray-700 dark:focus:ring-offset-gray-800 dark:text-gray-300 text-lg" do %>
            <span class="sr-only">Close</span>
            <i class="bi bi-x-lg"></i>
          <% end %>
        </div>
        <div class="p-4 overflow-y-auto max-h-[90vh]">
          <div class="sm:divide-y divide-gray-200 dark:divide-gray-700">
            <div class="py-3 sm:py-6">
              <%# <h4 class="mb-2 text-xs font-semibold uppercase text-gray-600 dark:text-gray-400">
                  Base
                </h4> %>
              <!-- Grid -->
              <%= content  %>
              <!-- End Grid -->
            </div>
          </div>
        </div>
        <div class="flex justify-end  bottom-0 items-center gap-x-2 py-3 px-4 border-t dark:border-gray-700">
          <%# <a class="inline-flex items-center gap-x-1.5 text-sm text-blue-600 decoration-2 hover:underline font-medium" href="../docs/index.html">
            Installation Guide
            <svg class="w-2.5 h-2.5" width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M5.27921 2L10.9257 7.64645C11.1209 7.84171 11.1209 8.15829 10.9257 8.35355L5.27921 14" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
            </svg>
          </a> %>
        </div>
      </div>
      <%# </div> %>
      <%# </div> %>
    </div>
  <% end %>
<% end %>
